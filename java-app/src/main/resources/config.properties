# ============================================================
#  Crafted Charms — Database Configuration
#  Copy to src/main/resources/ and update with your credentials.
#  NEVER commit real credentials to version control.
# ============================================================

# MySQL connection (update host/port if different)
db.url=jdbc:mysql://localhost:3306/crafted_charms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Your MySQL username
db.user=change_me

# Your MySQL password
db.password=change_me
